# Infrashift Dev Container Features

A collection of [Dev Container Features](https://containers.dev/implementors/features/) for Red Hat UBI-based development containers.

## Features

| Feature | Description | Usage |
|---------|-------------|-------|
| `ansible-core` | Installs ansible-core via UV tool | `ghcr.io/infrashift/devcontainer-features/ansible-core:latest` |
| `bun` | Installs the Bun JavaScript runtime | `ghcr.io/infrashift/devcontainer-features/bun:latest` |
| `claude-code` | Installs Claude Code (Anthropic's AI coding assistant CLI) | `ghcr.io/infrashift/devcontainer-features/claude-code:latest` |
| `cuelang` | Installs CUElang | `ghcr.io/infrashift/devcontainer-features/cuelang:latest` |
| `dotnet` | Installs the .NET SDK | `ghcr.io/infrashift/devcontainer-features/dotnet:latest` |
| `egress-filter` | Installs squid proxy and iptables for egress filtering | `ghcr.io/infrashift/devcontainer-features/egress-filter:latest` |
| `git` | Installs Git from source | `ghcr.io/infrashift/devcontainer-features/git:latest` |
| `git-lfs` | Installs Git LFS | `ghcr.io/infrashift/devcontainer-features/git-lfs:latest` |
| `golang` | Installs the Go programming language | `ghcr.io/infrashift/devcontainer-features/golang:latest` |
| `grype` | Installs the Grype vulnerability scanner | `ghcr.io/infrashift/devcontainer-features/grype:latest` |
| `jq` | Installs the jq JSON processor | `ghcr.io/infrashift/devcontainer-features/jq:latest` |
| `nodejs` | Installs Node.js | `ghcr.io/infrashift/devcontainer-features/nodejs:latest` |
| `npm` | Updates npm to a specific version | `ghcr.io/infrashift/devcontainer-features/npm:latest` |
| `openai-codex` | Installs OpenAI Codex CLI | `ghcr.io/infrashift/devcontainer-features/openai-codex:latest` |
| `openjdk` | Installs OpenJDK | `ghcr.io/infrashift/devcontainer-features/openjdk:latest` |
| `pnpm` | Installs the pnpm package manager | `ghcr.io/infrashift/devcontainer-features/pnpm:latest` |
| `python` | Installs a specific Python version via UV | `ghcr.io/infrashift/devcontainer-features/python:latest` |
| `syft` | Installs the Syft SBOM generator | `ghcr.io/infrashift/devcontainer-features/syft:latest` |
| `uv-ruff` | Installs UV and Ruff Python tools | `ghcr.io/infrashift/devcontainer-features/uv-ruff:latest` |
| `yq` | Installs the yq YAML processor | `ghcr.io/infrashift/devcontainer-features/yq:latest` |

## Usage

Add features to your `devcontainer.json`:

```jsonc
{
    "features": {
        "ghcr.io/infrashift/devcontainer-features/git:latest": {},
        "ghcr.io/infrashift/devcontainer-features/nodejs:latest": {},
        "ghcr.io/infrashift/devcontainer-features/python:latest": {
            "target_version": "3.12"
        }
    }
}
```

## Development

### Testing

Build the UBI9 base image and run tests:

```bash
# Test a single feature (autogenerated test)
make test-feature FEATURE=jq

# Test a feature with dependencies (scenario test)
make test-scenarios FEATURE=claude-code

# Run global integration test
make test-integration

# Run all tests
make test
```

## License

MIT
